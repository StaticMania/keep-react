import { TabsUnderline, TabsUnderlineCode } from './variant/TabsUnderline'
import { TabsUnderlineWithIcon, TabsUnderlineWithIconCode } from './variant/TabsUnderlineWithIcon'
import { DefaultTabs, DefaultTabsCode } from './variant/DefaultTabs'
import { TabsWithIcon, TabsWithIconCode } from './variant/TabsWithIcon'
import { tabsApiData, tabsItemApiData } from './tabsApi'

import CodeHighlightPreview from '../../../components/CodeHighlightPreview'
import ComponentApi from '../../../components/ComponentApi'

## Table of Contents

The Tabs Component in the Keep React allows you to organize and display content in a tabbed interface. It provides a convenient way to switch between different sections or views within a single container.

## Default Tabs

The Default Tabs component showcases the basic implementation of tabs.

<CodeHighlightPreview code={DefaultTabsCode}>
  <DefaultTabs />
</CodeHighlightPreview>

## Tabs Underline variant

Display tabs underline variant by setting the `variant` prop to `underline`.

<CodeHighlightPreview code={TabsUnderlineCode}>
  <TabsUnderline />
</CodeHighlightPreview>

## Tabs With Icon

<CodeHighlightPreview code={TabsWithIconCode}>
  <TabsWithIcon />
</CodeHighlightPreview>

## Tabs Underline Icon

<CodeHighlightPreview code={TabsUnderlineWithIconCode}>
  <TabsUnderlineWithIcon />
</CodeHighlightPreview>

## Tabs API Reference

Explore the available props for the `<Tabs>` component

<ComponentApi data={tabsApiData} />

## Tabs Items API Reference

Explore the available props for the `<TabsItem>` component

<ComponentApi data={tabsItemApiData} />
