import {
  DefaultDropdown,
  DefaultDropdownCode,
} from "./variant/DefaultDropdown";

import {
  DropdownSearchBar,
  DropdownSearchBarCode,
} from "./variant/DropdownSearchBar";

import {
  DropdownWithTitleDescription,
  DropdownWithTitleDescriptionCode,
} from "./variant/DropdownWithTitleDescription";
import {
  DropdownWithBothSideIcon,
  DropdownWithBothSideIconCode,
} from "./variant/DropdownWithBothSideIcon";
import {
  DropdownWithIconAndSearchBar,
  DropdownWithIconAndSearchBarCode,
} from "./variant/DropdownWithIconAndSearchBar";
import {
  DropdownWithTitleDesAndSearch,
  DropdownWithTitleDesAndSearchCode,
} from "./variant/DropdownWithTitleDesAndSearch";
import {
  DropdownWithLogo,
  DropdownWithLogoCode,
} from "./variant/DropdownWithLogo";

import {
  DropdownWithLogoSearchbar,
  DropdownWithLogoSearchbarCode,
} from "./variant/DropdownWithLogoSearchbar";
import {
  DropdownWithTitleDescriptionV3,
  DropdownWithTitleDescriptionV3Code,
} from "./variant/DropdownWithTitleDescriptionV3";
import {
  DropdownWithTitleDescriptionV4,
  DropdownWithTitleDescriptionV4Code,
} from "./variant/DropdownWithTitleDescriptionV4";
import {
  DropdownWithAvatar,
  DropdownWithAvatarCode,
} from "./variant/DropdownWithAvatar";
import {
  DropdownWithAvatarV2,
  DropdownWithAvatarV2Code,
} from "./variant/DropdownWithAvatarV2";
import {
  DropdownWithCheckbox,
  DropdownWithCheckboxCode,
} from "./variant/DropdownWithCheckbox";
import {
  DropdownWithCheckboxV2,
  DropdownWithCheckboxV2Code,
} from "./variant/DropdownWithCheckboxV2";
import {
  DropdownWithCheckboxV3,
  DropdownWithCheckboxV3Code,
} from "./variant/DropdownWithCheckboxV3";

import CodePreview from "~/components/CodePreview";
import CssThemePreview from "~/components/CssThemePreview";
import ComponentApi from "~/components/ComponentApi";
import { useTheme } from "~/src/Keep/ThemeContex";

## Table of Contents

The Dropdown Component is a versatile user interface element that provides users with a menu of selectable options. With a wide range of customization options, including labels, icons, search bars, checkboxes, and more, the Dropdown Component enhances user interaction by offering intuitive and flexible selection functionalities.

## Default Dropdown

The Default Dropdown represents some list items as a dropdown items with `<Dropdown.Item>` components and the `<Dropdown>` is a wrapper button where user can click to show the dropdown items. `dismissOnClick={true}` props means when the user select a dropdown item dropdown will be closed.

<CodePreview code={DefaultDropdownCode}>
  <DefaultDropdown />
</CodePreview>

## Dropdown With Search Bar

The Dropdown with Search Bar component features a search bar that allows users to filter and select from a list of fruit options.

<CodePreview code={DropdownSearchBarCode}>
  <DropdownSearchBar />
</CodePreview>

## Dropdown With Both Side Icon

The Dropdown with Both Side Icon component combines dropdown items with icons on both sides of the label.

<CodePreview code={DropdownWithBothSideIconCode}>
  <DropdownWithBothSideIcon />
</CodePreview>

## Dropdown With both side Icon And SearchBar

The Dropdown with Both Side Icon component combines dropdown items with search bar `<TextInput>` component.

<CodePreview code={DropdownWithIconAndSearchBarCode}>
  <DropdownWithIconAndSearchBar />
</CodePreview>

## Dropdown With Title Description

The dropdown with title description component represents the title and description in every dropdown items.

<CodePreview code={DropdownWithTitleDescriptionCode}>
  <DropdownWithTitleDescription />
</CodePreview>

## Dropdown Title Description Search Bar and Icon

This Dropdown component enhances the traditional dropdown by incorporating rich features. It introduces a search bar to swiftly locate options based on user input. Each option is presented with a descriptive title and additional information.

<CodePreview code={DropdownWithTitleDescriptionV3Code}>
  <DropdownWithTitleDescriptionV3 />
</CodePreview>

## Dropdown With Title Description And Search bar

This Dropdown component enhances the traditional dropdown by incorporating rich features. It introduces a search bar to swiftly locate options based on user input. Each option is presented with a descriptive title both side icons and additional information.

<CodePreview code={DropdownWithTitleDesAndSearchCode}>
  <DropdownWithTitleDesAndSearch />
</CodePreview>

## Dropdown With Logo

The Dropdown with Logo component integrates a company logo into the dropdown label.

<CodePreview code={DropdownWithLogoCode}>
  <DropdownWithLogo />
</CodePreview>

## Dropdown With Logo and Searchbar

The Dropdown with Logo and Searchbar component integrates a company logo into the dropdown label and a search bar.

<CodePreview code={DropdownWithLogoSearchbarCode}>
  <DropdownWithLogoSearchbar />
</CodePreview>

## Dropdown Title Description Logo and Searchbar

The dropdown with title description component represents the title and description in every dropdown items with logo and a search bar.

<CodePreview code={DropdownWithTitleDescriptionV4Code}>
  <DropdownWithTitleDescriptionV4 />
</CodePreview>

## Dropdown With Avatar

The Dropdown with Avatar component integrates a user `<Avatar>` component into the dropdown label.

<CodePreview code={DropdownWithAvatarCode}>
  <DropdownWithAvatar />
</CodePreview>

## Dropdown With Avatar and Searchbar

The Dropdown with Avatar and Searchbar component combines a user `<Avatar>` component and a search bar within the dropdown label.

<CodePreview code={DropdownWithAvatarV2Code}>
  <DropdownWithAvatarV2 />
</CodePreview>

## Dropdown With Checkbox

The Dropdown with Checkbox component integrates `<CheckBox>` component within the dropdown menu, enabling users to select multiple options simultaneously.

<CodePreview code={DropdownWithCheckboxCode}>
  <DropdownWithCheckbox />
</CodePreview>

## Dropdown With Checkbox SearchBar

The Dropdown with Checkbox and SearchBar component combines `<CheckBox>` component with a search bar within the dropdown menu.

<CodePreview code={DropdownWithCheckboxV2Code}>
  <DropdownWithCheckboxV2 />
</CodePreview>

## Dropdown Title Description With Checkbox SearchBar

The Dropdown with Title, Description, Checkbox, and SearchBar component offers a comprehensive user experience by combining rich features. It provides descriptive titles and additional information for options, allowing users to make informed choices.

<CodePreview code={DropdownWithCheckboxV3Code}>
  <DropdownWithCheckboxV3 />
</CodePreview>
