import { DefaultStep, DefaultStepCode } from "./variant/DefaultStep";
import { StepWithoutBg, StepWithoutBgCode } from "./variant/StepWithoutBg";
import { StepWithRing, StepWithRingCode } from "./variant/StepWithRing";
import { ColorsOfStep, ColorsOfStepCode } from "./variant/ColorsOfStep";
import {
  DashedBorderStep,
  DashedBorderStepCode,
} from "./variant/DashedBorderStep";
import {
  IncompletedStep,
  IncompletedStepCode,
} from "./variant/IncompletedStep";
import { stepApiData } from "./StepApi";

import CodePreview from "~/components/CodePreview";
import CssThemePreview from "~/components/CssThemePreview";
import ComponentApi from "~/components/ComponentApi";
import { useTheme } from "~/src/Keep/ThemeContex";

## Table of Contents

Steps Component is a user interface element used to guide users through a multi-step process or workflow. It presents a visual representation of the various stages or steps a user needs to complete in a sequential order. Each step is typically accompanied by a title, description, and an indicator of completion status, helping users track their progress.

## Default Step

The Default Step refers to the fundamental and default configuration of a Steps Component within a user interface. It provides a clear visual representation of each step in a multi-step process.

<CodePreview github="Step" code={DefaultStepCode}>
  <DefaultStep />
</CodePreview>

## Color variant of Step

The Step component comes in various color variants to suit your design needs. By adjusting the `color` prop, you can choose from our color options to represent the status or significance of each step. Available colors: `"info" | "error" | "success" | "warning" | "gray"`

<CodePreview github="Step" code={ColorsOfStepCode}>
  <ColorsOfStep />
</CodePreview>

## Incompleted Step

An Incomplete Step refers to a step within a multi-step process or workflow that has not yet been completed by the user.

<CodePreview github="Step" code={IncompletedStepCode}>
  <IncompletedStep />
</CodePreview>

## Step Without Background color

A Step Without Background Color is a step within a Steps Component that lacks a distinct background color to indicate its status. By adjusting the `withBg={true | false}` props, we can observe the two different variants of the step component.

<CodePreview github="Step" code={StepWithoutBgCode}>
  <StepWithoutBg />
</CodePreview>

## Step With Ring

Add a ring around a step component by setting the withRing prop to true.

<CodePreview github="Step" code={StepWithRingCode}>
  <StepWithRing />
</CodePreview>

## Step With Dashed Border

To change the border style between two steps and apply a dashed line, use the `borderType="dashed"` prop.

<CodePreview github="Step" code={DashedBorderStepCode}>
  <DashedBorderStep />
</CodePreview>

## API Reference

Explore the available props for the Step component

<ComponentApi data={stepApiData} />

## Theme

To learn more about how to customize the appearance of components, please see the Theme docs.

<CssThemePreview code={useTheme().theme.stepper} />
