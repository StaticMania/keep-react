import { keepSkeletonTheme } from './Skeleton'

export const skeletonTheme: keepSkeletonTheme = {
  base: 'w-full',
  animation: 'animate-pulse',
  line: 'mb-2.5  w-full bg-slate-50 rounded-md',
  avatar: 'h-[60px] w-[60px] rounded-full bg-slate-50',
}
